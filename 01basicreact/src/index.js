import React from 'react';                    // React Library to import!
import ReactDOM from 'react-dom/client';      // Imported library of react that is used to implement on web
import App from './App'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  
    <App />

);

